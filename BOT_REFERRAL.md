# Реферальная система — инструкция для разработчика Telegram-бота

Базовый URL API: тот же, что у фронта (например `https://твой-бэкенд.up.railway.app`).

---

## Что такое реферальная ссылка и код

У каждого пользователя есть **уникальный 6-значный реферальный код** (например `K7MN2P`). Делиться можно и ссылкой, и просто кодом.

- **Ссылка:** `https://t.me/ИМЯ_БОТА?start=ref_K7MN2P` — после `ref_` идёт 6-значный код.
- **Код:** сам код `K7MN2P` друг может ввести вручную (бот может спросить «Введи код друга» и отправить на бэкенд в поле `ref` при регистрации/входе).

В Telegram при переходе по ссылке бот получает параметр (payload) = **`ref_K7MN2P`**. Эту строку целиком (`ref_...`) или только код (`K7MN2P`) нужно передавать на бэкенд в поле `ref` при регистрации/входе приглашённого.

---

## ⚠️ Как Telegram передаёт реферальный код (важно)

**Telegram не присылает отдельное поле «start».** Вся строка после `?start=` оказывается **в тексте сообщения** — в одном поле с командой `/start`.

При переходе по ссылке `https://t.me/ИМЯ_БОТА?start=ref_K7MN2P` в update приходит:

- **`message.text`** = **`"/start ref_K7MN2P"`** — команда и параметр через **пробел** в одной строке.

Если бот обрабатывает только «команда /start» и не читает остаток строки, реферальный код теряется. Нужно **всегда брать параметр из текста**.

**Пример извлечения параметра (JavaScript / Node.js):**

```js
// Обработчик команды /start
bot.onText(/\/start/, (msg) => {
  const text = msg.text || '';           // "/start ref_K7MN2P" или просто "/start"
  const refPayload = text.trim().split(/\s+/)[1] || null;  // "ref_K7MN2P" или null

  if (refPayload) {
    // Пользователь пришёл по реферальной ссылке — сохрани refPayload
    // (в память по chat_id или в БД) и подставь в ref при register/login
    saveRefForChat(msg.chat.id, refPayload);  // твоя функция
  }
  // ... остальная логика приветствия
});
```

**Пример (Python, python-telegram-bot):**

```python
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    text = (update.message.text or "").strip()   # "/start ref_K7MN2P"
    parts = text.split(maxsplit=1)
    ref_payload = parts[1] if len(parts) > 1 else None   # "ref_K7MN2P" или None

    if ref_payload:
        # сохранить ref_payload по update.effective_chat.id
        save_ref_for_chat(update.effective_chat.id, ref_payload)
```

**Проверка:** при открытии ссылки `https://t.me/ТВОЙ_БОТ?start=ref_K7MN2P` выведи в лог `message.text` — должно быть ровно `"/start ref_K7MN2P"`. Если видишь только `"/start"`, значит ссылка собирается без параметра (проверь, что в ссылке действительно есть `?start=ref_КОД`).

---

## Сценарий 1: Пригласивший нажимает «Пригласить друга»

1. Пользователь уже авторизован в боте (есть токен с бэкенда).
2. Он нажимает кнопку типа **«👥 Пригласить друга»**.
3. Бот вызывает бэкенд и показывает:
   - реферальную ссылку;
   - текст: «Ты получишь X баллов, когда друг зарегистрируется и сделает 1-й спин»;
   - кнопку «Поделиться» (шаринг этой ссылки).

**Что вызвать на бэкенде:**

- **GET** `/api/players/me`  
  Заголовок: `Authorization: Bearer <токен_игрока>`

**Что приходит в ответе (для игрока):**

```json
{
  "_id": "...",
  "phone": "79991234567",
  "name": "Иван",
  "balance": 100,
  "referralCode": "K7MN2P",
  "referralLink": "https://t.me/your_bot?start=ref_K7MN2P",
  "referralPointsPerFriend": 5
}
```

- **`referralCode`** — **6-значный код** пользователя (например `K7MN2P`). Его можно показывать отдельно: «Твой код для друзей: **K7MN2P**» — друг может ввести код вручную, без перехода по ссылке.
- **`referralLink`** — готовая ссылка. Если в `.env` не задан `TELEGRAM_BOT_USERNAME`, поле будет `null` — тогда собирай сам: `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=ref_` + `referralCode`.
- **`referralPointsPerFriend`** — число X из фразы «получишь X баллов» (по умолчанию 5).

**Что показать пользователю:**

- Текст: «Твой код для друзей: **{referralCode}**» и/или «Ссылка:» + `referralLink`.
- Текст: «Ты получишь **{referralPointsPerFriend}** баллов, когда друг зарегистрируется и сделает 1-й спин».
- Кнопка «Поделиться» — шаринг ссылки или кода.

---

## Сценарий 2: Вход и регистрация (код друга при регистрации)

**Логика бота при получении контакта (телефон + код):**

1. Вызвать **только** **POST** `/api/auth/login` с телом `{ "phone", "code" }` — **без** `ref`.
2. **Если бэкенд вернул 200 и токен** — пользователь уже есть → показать «Вы вошли», дальше обычный сценарий.
3. **Если бэкенд вернул 404** (пользователь не найден) — запустить **регистрацию**:
   - Запросить **код друга** (кто пригласил), с кнопкой **«Пропустить»** (можно не вводить).
   - Запросить **имя**.
   - Вызвать **POST** `/api/auth/register` с телом `{ "phone", "code", "name", "ref" }`. Если пользователь ввёл код — передать `ref` (строка `ref_КОД` или просто `КОД`); если нажал «Пропустить» — не передавать поле `ref` или передать `null`.

**Ответ login при ненайденном пользователе (404):**

```json
{
  "message": "Пользователь не найден. Зарегистрируйтесь.",
  "code": "USER_NOT_FOUND"
}
```

По полю `code: "USER_NOT_FOUND"` бот понимает, что нужно запустить регистрацию (код друга → имя → register).

**Регистрация (новый пользователь):**

- **POST** `/api/auth/register`  
  Тело (JSON):

```json
{
  "phone": "79991234567",
  "code": "0000",
  "name": "Марья",
  "ref": "K7MN2P"
}
```

или без реферала (пропустил): `{ "phone", "code", "name" }` (поле `ref` не передавать).

- **`ref`** — 6-значный код друга или `ref_КОД`. Только если пользователь ввёл код (не нажал «Пропустить»).
- В ответе: `token`, `_id`, `phone`, `name`, `balance` и т.д.

**Дальше по сценарию:** бот может попросить геолокацию/подтверждение клуба и предложить крутить рулетку. После **первого платного спина** бэкенд сам начислит баллы пригласившему.

---

## Кратко: что передавать на бэкенд

| Действие | Метод | URL | Тело / заголовки |
|----------|--------|-----|-------------------|
| Получить ссылку и X баллов для пригласившего | GET | `/api/players/me` | Заголовок: `Authorization: Bearer <токен>` |
| Вход (без ref; при 404 — запустить регистрацию) | POST | `/api/auth/login` | `{ "phone", "code" }` |
| Регистрация (код друга опционален) | POST | `/api/auth/register` | `{ "phone", "code", "name", "ref"?: "КОД" или "ref_КОД" }` |

---

## Важно для бота

1. **Откуда брать `ref`**  
   При регистрации — из ввода пользователя (код друга) или кнопки «Пропустить». Передавать в поле `ref` только в **register**; в **login** `ref` не передаётся. Бэкенд принимает и `ref_КОД`, и просто `КОД`.

2. **Когда передавать `ref`**  
   Только при вызове **register** (если пользователь ввёл код друга). При **login** пользователь не создаётся — при 404 бот запрашивает код друга и имя и вызывает **register**.

3. **Когда начисляются баллы**  
   Не при регистрации и не при входе. Бэкенд начисляет баллы пригласившему **после первого платного спина** приглашённого. Бот для этого ничего отдельно не дергает.

4. **Если нет `referralLink` в ответе**  
   На бэкенде не задан `TELEGRAM_BOT_USERNAME`. Собирай ссылку сам:  
   `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=ref_` + значение поля `referralCode` (6-значный код).

Если что-то останется непонятно — можно уточнить по конкретному шагу (например: «что именно приходит в update при переходе по ссылке» или «какой точно JSON слать в register»).
