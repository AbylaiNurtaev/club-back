# Реферальная система — инструкция для разработчика Telegram-бота

Базовый URL API: тот же, что у фронта (например `https://твой-бэкенд.up.railway.app`).

---

## Что такое реферальная ссылка и код

У каждого пользователя есть **уникальный 6-значный реферальный код** (например `K7MN2P`). Делиться можно и ссылкой, и просто кодом.

- **Ссылка:** `https://t.me/ИМЯ_БОТА?start=ref_K7MN2P` — после `ref_` идёт 6-значный код.
- **Код:** сам код `K7MN2P` друг может ввести вручную (бот может спросить «Введи код друга» и отправить на бэкенд в поле `ref` при регистрации/входе).

В Telegram при переходе по ссылке бот получает в команде `/start` параметр (payload) = **`ref_K7MN2P`**. Эту строку целиком (`ref_...`) или только код (`K7MN2P`) нужно передавать на бэкенд в поле `ref` при регистрации/входе приглашённого.

---

## Сценарий 1: Пригласивший нажимает «Пригласить друга»

1. Пользователь уже авторизован в боте (есть токен с бэкенда).
2. Он нажимает кнопку типа **«👥 Пригласить друга»**.
3. Бот вызывает бэкенд и показывает:
   - реферальную ссылку;
   - текст: «Ты получишь X баллов, когда друг зарегистрируется и сделает 1-й спин»;
   - кнопку «Поделиться» (шаринг этой ссылки).

**Что вызвать на бэкенде:**

- **GET** `/api/players/me`  
  Заголовок: `Authorization: Bearer <токен_игрока>`

**Что приходит в ответе (для игрока):**

```json
{
  "_id": "...",
  "phone": "79991234567",
  "name": "Иван",
  "balance": 100,
  "referralCode": "K7MN2P",
  "referralLink": "https://t.me/your_bot?start=ref_K7MN2P",
  "referralPointsPerFriend": 50
}
```

- **`referralCode`** — **6-значный код** пользователя (например `K7MN2P`). Его можно показывать отдельно: «Твой код для друзей: **K7MN2P**» — друг может ввести код вручную, без перехода по ссылке.
- **`referralLink`** — готовая ссылка. Если в `.env` не задан `TELEGRAM_BOT_USERNAME`, поле будет `null` — тогда собирай сам: `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=ref_` + `referralCode`.
- **`referralPointsPerFriend`** — число X из фразы «получишь X баллов» (обычно 50).

**Что показать пользователю:**

- Текст: «Твой код для друзей: **{referralCode}**» и/или «Ссылка:» + `referralLink`.
- Текст: «Ты получишь **{referralPointsPerFriend}** баллов, когда друг зарегистрируется и сделает 1-й спин».
- Кнопка «Поделиться» — шаринг ссылки или кода.

---

## Сценарий 2: Приглашённый переходит по ссылке и регистрируется

1. Друг переходит по ссылке вида `https://t.me/бот?start=ref_K7MN2P` **или** вводит код друга (например `K7MN2P`) в боте.
2. Если по ссылке — в update приходит параметр `start` = **`ref_K7MN2P`**. Сохрани его (или только код `K7MN2P`) в контексте чата.
3. Бот просит: логин (имя), телефон (для регистрации на бэкенде).
4. Бот отправляет на бэкенд запрос регистрации **и в теле передаёт ref**: либо `ref_K7MN2P`, либо просто `K7MN2P`.

**Регистрация (новый пользователь):**

- **POST** `/api/auth/register`  
  Тело (JSON):

```json
{
  "phone": "79991234567",
  "code": "0000",
  "name": "Марья",
  "ref": "ref_K7MN2P"
}
```

или с кодом без префикса: `"ref": "K7MN2P"`.

- **`ref`** — либо строка из параметра `start` (`ref_...`), либо только 6-значный код. Передавать только когда пользователь пришёл по ссылке или ввёл код друга.
- В ответе: `token`, `_id`, `phone`, `name`, `balance` и т.д.

**Если пользователь не регистрируется, а только входит (уже есть в системе):**

- **POST** `/api/auth/login`  
  Тело: `{ "phone": "79991234567", "code": "0000", "ref": "K7MN2P" }` (или `"ref": "ref_K7MN2P"`).  
  При первом создании пользователя по логину бэкенд привяжет реферера по `ref`. Если пользователь уже был — `ref` не перезапишет реферера.

**Дальше по сценарию:** бот может попросить геолокацию/подтверждение клуба (как у вас уже есть) и предложить крутить рулетку. После **первого платного спина** бэкенд сам начислит баллы пригласившему — боту ничего дополнительно вызывать не нужно.

---

## Кратко: что передавать на бэкенд

| Действие | Метод | URL | Тело / заголовки |
|----------|--------|-----|-------------------|
| Получить ссылку и X баллов для пригласившего | GET | `/api/players/me` | Заголовок: `Authorization: Bearer <токен>` |
| Регистрация с рефералом | POST | `/api/auth/register` | `{ "phone", "code", "name", "ref": "ref_КОД" или "ref": "КОД" }` |
| Вход (с рефералом при первом создании юзера) | POST | `/api/auth/login` | `{ "phone", "code", "ref": "ref_КОД" или "ref": "КОД" }` |

---

## Важно для бота

1. **Откуда брать `ref`**  
   Из параметра команды `/start` при переходе по ссылке (`ref_КОД`) или из ввода пользователя (код без префикса). Сохрани значение в контексте чата и передай в поле `ref` при первом вызове регистрации/логина. Бэкенд принимает и `ref_КОД`, и просто `КОД`.

2. **Когда передавать `ref`**  
   Только один раз — при первой регистрации или первом входе пользователя, который пришёл по реферальной ссылке. Повторно передавать не нужно; бэкенд не перезаписывает уже привязанного реферера.

3. **Когда начисляются баллы**  
   Не при регистрации и не при входе. Бэкенд начисляет баллы пригласившему **после первого платного спина** приглашённого. Бот для этого ничего отдельно не дергает.

4. **Если нет `referralLink` в ответе**  
   На бэкенде не задан `TELEGRAM_BOT_USERNAME`. Собирай ссылку сам:  
   `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=ref_` + значение поля `referralCode` (6-значный код).

Если что-то останется непонятно — можно уточнить по конкретному шагу (например: «что именно приходит в update при переходе по ссылке» или «какой точно JSON слать в register»).
