# Реферальная система — инструкция для разработчика Telegram-бота

Базовый URL API: тот же, что у фронта (например `https://твой-бэкенд.up.railway.app`).

---

## Что такое реферальная ссылка

В Telegram при нажатии на ссылку пользователь переходит в бота, и бот получает **payload** из команды `/start`:

- Ссылка: `https://t.me/ИМЯ_БОТА?start=ref_1234567890abcdef12345678`
- Бот получает: команда `start`, параметр (payload) = **`ref_1234567890abcdef12345678`**

Часть после `ref_` — это **id пользователя, который пригласил** (24 символа, hex). Эту строку целиком (`ref_...`) нужно передавать на бэкенд при регистрации/входе приглашённого.

---

## Сценарий 1: Пригласивший нажимает «Пригласить друга»

1. Пользователь уже авторизован в боте (есть токен с бэкенда).
2. Он нажимает кнопку типа **«👥 Пригласить друга»**.
3. Бот вызывает бэкенд и показывает:
   - реферальную ссылку;
   - текст: «Ты получишь X баллов, когда друг зарегистрируется и сделает 1-й спин»;
   - кнопку «Поделиться» (шаринг этой ссылки).

**Что вызвать на бэкенде:**

- **GET** `/api/players/me`  
  Заголовок: `Authorization: Bearer <токен_игрока>`

**Что приходит в ответе (для игрока):**

```json
{
  "_id": "...",
  "phone": "79991234567",
  "name": "Иван",
  "balance": 100,
  "referralCode": "ref_507f1f77bcf86cd799439011",
  "referralLink": "https://t.me/your_bot?start=ref_507f1f77bcf86cd799439011",
  "referralPointsPerFriend": 50
}
```

- **`referralLink`** — готовая ссылка, которую можно показать и отправить другу. Если в `.env` бэкенда не задан `TELEGRAM_BOT_USERNAME`, поле будет `null` — тогда ссылку собирай сам: `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=referralCode`.
- **`referralCode`** — код вида `ref_<userId>`, тот же, что в ссылке после `start=`.
- **`referralPointsPerFriend`** — число X из фразы «получишь X баллов» (обычно 50).

**Что показать пользователю:**

- Текст: «Твоя ссылка для приглашения:» + `referralLink` (или собранная ссылка).
- Текст: «Ты получишь **{referralPointsPerFriend}** баллов, когда друг зарегистрируется и сделает 1-й спин».
- Кнопка «Поделиться» — системный шаринг ссылки (в Telegram это можно сделать через `switch_inline_query` или просто скопировать ссылку).

---

## Сценарий 2: Приглашённый переходит по ссылке и регистрируется

1. Друг переходит по ссылке вида `https://t.me/бот?start=ref_507f1f77bcf86cd799439011`.
2. Открывается бот. В обновлении (update) приходит **deep_link**: параметр `start` = **`ref_507f1f77bcf86cd799439011`** — его нужно сохранить (в памяти бота для этого чата или в БД по chat_id).
3. Бот просит: логин (имя), телефон (для регистрации на бэкенде).
4. Бот отправляет на бэкенд запрос регистрации **и в теле передаёт сохранённый ref**.

**Регистрация (новый пользователь):**

- **POST** `/api/auth/register`  
  Тело (JSON):

```json
{
  "phone": "79991234567",
  "code": "0000",
  "name": "Марья",
  "ref": "ref_507f1f77bcf86cd799439011"
}
```

- **`ref`** — строка **как есть** из параметра `start` (весь payload: `ref_...`). Обязательно передавать только когда пользователь пришёл по реферальной ссылке.
- В ответе: `token`, `_id`, `phone`, `name`, `balance` и т.д. — дальше работаешь с пользователем как с обычным залогиненным.

**Если пользователь не регистрируется, а только входит (уже есть в системе):**

- **POST** `/api/auth/login`  
  Тело: `{ "phone": "79991234567", "code": "0000", "ref": "ref_507f1f77bcf86cd799439011" }`  
  Если по этому телефону пользователь создаётся впервые (авто-регистрация при логине), бэкенд привяжет реферера по `ref`. Если пользователь уже был — `ref` не перезапишет существующего реферера.

**Дальше по сценарию:** бот может попросить геолокацию/подтверждение клуба (как у вас уже есть) и предложить крутить рулетку. После **первого платного спина** бэкенд сам начислит баллы пригласившему — боту ничего дополнительно вызывать не нужно.

---

## Кратко: что передавать на бэкенд

| Действие | Метод | URL | Тело / заголовки |
|----------|--------|-----|-------------------|
| Получить ссылку и X баллов для пригласившего | GET | `/api/players/me` | Заголовок: `Authorization: Bearer <токен>` |
| Регистрация с рефералом | POST | `/api/auth/register` | `{ "phone", "code", "name", "ref": "ref_..." }` — ref из `start` |
| Вход (с рефералом при первом создании юзера) | POST | `/api/auth/login` | `{ "phone", "code", "ref": "ref_..." }` |

---

## Важно для бота

1. **Откуда брать `ref`**  
   Из параметра команды `/start`: в Telegram при переходе по ссылке `?start=ref_xxx` в update приходит именно `ref_xxx`. Сохрани это значение в контексте чата (или в пользователе по `chat_id`) и подставь в поле `ref` при первом вызове регистрации/логина для этого пользователя.

2. **Когда передавать `ref`**  
   Только один раз — при первой регистрации или первом входе пользователя, который пришёл по реферальной ссылке. Повторно передавать не нужно; бэкенд не перезаписывает уже привязанного реферера.

3. **Когда начисляются баллы**  
   Не при регистрации и не при входе. Бэкенд начисляет баллы пригласившему **после первого платного спина** приглашённого. Бот для этого ничего отдельно не дергает.

4. **Если нет `referralLink` в ответе**  
   Значит на бэкенде не задан `TELEGRAM_BOT_USERNAME`. Собирай ссылку сам:  
   `https://t.me/ИМЯ_ТВОЕГО_БОТА?start=` + значение поля `referralCode` из ответа GET `/api/players/me`.

Если что-то останется непонятно — можно уточнить по конкретному шагу (например: «что именно приходит в update при переходе по ссылке» или «какой точно JSON слать в register»).
